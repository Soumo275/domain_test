services:
  app:
    build: ./
    container_name: startup-node-app
    expose:
      - "3000"
    restart: unless-stopped
    networks:
      - web

  nginx:
    image: nginx:alpine
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs:/etc/ssl/certs:ro
      - ./nginx/private:/etc/ssl/private:ro
    depends_on:
      - app
    restart: unless-stopped
    networks:
      - web
networks:
  web:
    driver: bridge
